/**
 * Placeholder root view. API base URL is read from import.meta.env.VITE_API_URL
 * (default in dev: http://localhost:5001/api).
 */
export function HomePage() {
  return (
    <main style={{ padding: "2rem" }}>
      <h1>FinPlanner</h1>
    </main>
  );
}
